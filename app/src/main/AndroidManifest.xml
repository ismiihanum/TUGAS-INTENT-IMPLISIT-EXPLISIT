<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.latihan5">

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />


    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="Intent Explisit Implisit"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <!--ini  fungsinya buat pertama yang akan keluar itu yang mana, misal klo buku itu cover paling depan-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".IntentBundleActivity">
        </activity>
        <activity android:name=".ImplisitIntentActivity">
        </activity>
        <activity android:name=".ExplisitIntentActivity">
        </activity>
        <activity android:name=".WebViewActivity">

        </activity>
    </application>





</manifest>